<!-- bmad:context -->
<!-- Verified 2026-09-02 against 28643d4. Managed by bmad-project-context; edits inside this block are replaced on refresh. Keep anything you want preserved outside the markers. -->

## CherkasyVolley

Публічна платформа турнірів Федерації волейболу Черкащини. Стек за планом (код ще не написано):
Next.js 16 (App Router) + TypeScript, pnpm, Prisma + PostgreSQL (Neon), Better Auth (Google),
shadcn/ui + Tailwind, хостинг Vercel. Планування живе в `_bmad-output/` — архітектурний спайн
`_bmad-output/planning-artifacts/architecture/architecture-untitled-2026-09-02/ARCHITECTURE-SPINE.md`
є джерелом істини для інваріантів; історії — `_bmad-output/planning-artifacts/epics.md`.

## Policy

- Секрети (`DATABASE_URL`, Google OAuth) — лише через змінні середовища, ніколи в git.
- Схему БД змінювати лише міграціями Prisma; довідкові дані й першого адміна — лише seed-скриптом.
- Не редагувати згенерований клієнт Prisma вручну — перегенерувати через `pnpm prisma generate`.

## Where things are

- Інваріанти архітектури (AD-1…AD-11): `ARCHITECTURE-SPINE.md` — читати перед структурними змінами.
- Історії для реалізації: `_bmad-output/planning-artifacts/epics.md`; статус — `_bmad-output/implementation-artifacts/sprint-status.yaml`.
- UX-контракти: `_bmad-output/planning-artifacts/ux-designs/ux-untitled-2026-09-02/DESIGN.md` (візуал) і `EXPERIENCE.md` (поведінка, IA, стани).
- Плановий контракт: `_bmad-output/specs/spec-cherkasy-volley/SPEC.md`.

## Running and verifying

- Пакетний менеджер — **pnpm** (`pnpm install`, `pnpm dev`, `pnpm build`, `pnpm lint`); Node 24.
- `pnpm lint` (ESLint 9 flat config) включає правила меж імпорту: `src/domain/` без `next`/Prisma-клієнта/`react`/інших шарів; Prisma-клієнт (`@/generated/prisma`, `@prisma/client`) лише в `src/data/`; `src/data/` не залежить від `actions`/`auth`/view/`next`/`react`; `src/auth/` не залежить від `domain`/`actions`/view і бере Prisma-клієнт лише через `src/data/` (`next/*` і `better-auth/*` дозволені). Ловляться і alias-, і відносні форми. Порушення — помилка ESLint, окремо від перевірки типів.
- `pnpm test` (`vitest run`) — юніт-тести доменних функцій `src/domain/*` (Vitest, `environment: node`, `src/**/*.test.ts`, конфіг `vitest.config.mts`); `pnpm test:watch` для розробки. Обовʼязкові для кожної доменної функції. Не входить у `build` (нема CI).
- Міграції: `pnpm prisma migrate dev --name <...>` для розробки — **проти власної Neon-гілки, не проти прод-гілки** (може запропонувати reset, створює/видаляє shadow DB). Прод отримує міграції лише через `pnpm prisma migrate deploy`, який `build` запускає через `scripts/migrate-deploy.mjs` (пропускається на preview-збірках Vercel). `prisma generate` — у `postinstall` і на початку `build`, бо `src/generated/` gitignored.
  - **`migrate dev` не працює у не-інтерактивному інструменті**, якщо зміна схеми дає попередження (напр. «a unique constraint … will be added») — попередження потребує інтерактивного підтвердження, `--create-only` не оминає. Фолбек (Story 1.5 / 2.1 / 2.4): pre-flight `prisma migrate diff --from-config-datasource --to-schema prisma/schema.prisma --script` → руками записати `migration.sql` у `prisma/migrations/<YYYYMMDDHHMMSS>_<name>/` дослівно → `prisma migrate deploy` (не інтерактивний). Перевірити `migrate status` + `migrate diff --exit-code`.
- Seed: `pnpm seed` (`prisma db seed` → `tsx prisma/seed.mts`) — ідемпотентний upsert першого адміна по `SEED_ADMIN_EMAIL` (у нижньому регістрі). Повторний запуск дублів не створює.
- `pnpm typecheck` (`tsc --noEmit`) — офлайн-перевірка типів (`next build` тепер потребує БД). Після додавання **нового маршруту** `tsc` падає на `PageProps<"/new/route">` / `LayoutProps<…>`, доки `next build` (або `next dev`) не перегенерує `.next/types` — спочатку `pnpm build`, потім `typecheck` зелений.
- `pnpm exec tsx scripts/verify-admin-roles.mts` — регрес-перевірка шару ролей (`src/data/users.ts`): гілки `not_found` / `last_admin` / ідемпотентність. Не руйнівна (адмінів не знімає).
- `pnpm exec tsx scripts/verify-tournament-create.mts` — регрес-перевірка створення турніру (`src/data/tournaments.ts`): `state = DRAFT`, рівно одна `Group`, збережений пресет, дубль → `P2002`. Само-прибиральний (створює й видаляє тимчасовий турнір; лишає БД як була).
- `pnpm exec tsx scripts/verify-match-schedule.mts` — регрес-перевірка розкладу матчів (`src/data/matches.ts`): `updateMatchSchedule` скоупиться за `(tournamentId, matchId, stage:"GROUP")` (крос-турнірна пара й `SEMIFINAL`-матч → `count 0`), внесений `SetScore` переживає перепланування, `listGroupMatchesForTournament` віддає лише `GROUP`-матчі в хронологічному порядку. Само-прибиральний (два тимчасові турніри, повний teardown).
- `pnpm exec tsx scripts/verify-match-result.mts` — регрес-перевірка внесення результату (`src/data/matches.ts`): `createMatchResult` для `CLASSIC` і `CUSTOM` турнірів → `getStandings` відображає очки/партії за пресетом; повторний виклик → `reason:"exists"`; крос-турнірна пара й `SEMIFINAL`-матч → `reason:"not_found"`. Само-прибиральний (два тимчасові турніри, повний teardown).
- `pnpm exec tsx scripts/verify-edit-delete-result.mts` — регрес-перевірка виправлення/видалення результату (`src/data/matches.ts`): `replaceMatchResult` (3:0 → 3:2) змінює `getStandings` (3/0 → 2/1); `deleteMatchResult` прибирає матч із таблиці (`played: 0`); крос-турнірна пара й `SEMIFINAL` → `not_found` / `count 0`. Само-прибиральний.
- `pnpm exec tsx scripts/verify-advance-bracket.mts` — регрес-перевірка автопросування сітки (`src/data/playoff.ts` `savePlayoffAdvancement` / `getPlayoffBracket`) + фінальних місць і гейта правки півфіналу (Story 4.4): 4-командний турнір, жеребкування, усі групові результати, формування плейофа, тоді ввід/правка/видалення результатів півфіналів → `FINAL`/`THIRD_PLACE` рядки створюються, оновлюються, заморожуються після власного результату, обнуляються при знятті результату півфіналу; `getPlayoffBracket().placements` `null` доки вирішальний матч не зіграно, потім віддають назви команд; `checkCanEditSemifinalResult` дозволяє правку доки downstream не зіграно й блокує після; проба тіснішого `match_slot_stage_check`. Само-прибиральний.
- `pnpm exec tsx scripts/verify-finish-tournament.mts` — регрес-перевірка завершення турніру (Story 4.5): 4-командний турнір до плейофа, обидва півфінали. `finalAndThirdPlacePlayed` (`src/data/matches.ts`) `false` доки не зіграно і Фінал, і Матч за 3-тє (лише Фінал — недостатньо), потім `true`; `checkTransition(PLAYOFF→COMPLETED)` відмовляє до, дозволяє після; `checkCanEditResults` блокує правку результатів лише в `COMPLETED`; після `setTournamentState(…, COMPLETED)` перезавантажений стан вмикає замок. Само-прибиральний (повний teardown).
- `pnpm exec tsx scripts/verify-generate-playoff.mts` — регрес-перевірка формування плейофа (`src/data/playoff.ts`, `allGroupMatchesPlayed`): два 4-командні турніри. (1) чиста таблиця → `allGroupMatchesPlayed` false→true; `checkTransition` відмовляє; `savePlayoffFormation` → 2 `SEMIFINAL` (`slot` SF1/SF2, посів 1v4 / 2v3), `state → PLAYOFF`; повторний виклик → `{ ok:false, reason:"already_formed" }`; CHECK `match_slot_stage_check` відхиляє playoff-матч без `slot` і GROUP-матч зі `slot`. (2) 3-way цикл угорі → `seedPlayoff().needsManualSeed`; видалення `SetScore` в процесі → `savePlayoffFormation` → `group_incomplete`, нічого не записано. Само-прибиральний.

## Conventions that differ from defaults

- `src/domain/` — чисті функції: не імпортує `next`, Prisma-клієнт, `react`, `src/data`, `src/actions`, `src/auth`, `src/app`, `src/components`, `src/lib`.
- Prisma-клієнт (`@/generated/prisma`; `@prisma/client` — реекспорт) імпортується лише в `src/data/`; усі читання/записи — через іменовані функції там. `src/auth/` бере спільний інстанс клієнта з `src/data/`.
- Кожна мутація даних — Server Action у `src/actions/`, перший рядок `await requireAdmin()` (кидає `AdminRequiredError` → мапиться в `{ ok: false, code, message }`); `/admin/**` гейтиться в `src/app/admin/layout.tsx` через `requireAdminPage()` (редіректить). Тост-примітив — `sonner` (`<Toaster />` у `src/app/layout.tsx`).
- Багаторазові UI-примітиви (Story 2.2, `src/components/README.md`): `notify` (`src/lib/notify.ts` — завжди він, не `toast` напряму, крім `ui/sonner.tsx` і `flash-toaster.tsx`); `ConfirmDialog` (`src/components/confirm-dialog.tsx`, обгортка над `ui/dialog`; `onConfirm` кидає ⇒ діалог лишається відкритим, тост не показує — месидж на боці caller); `EmptyState` + `src/lib/empty-states.ts` (єдине джерело копірайту); `Skeleton`/`TableSkeleton`/`CardSkeleton` (`src/components/skeletons.tsx` — жодних повносторінкових спінерів). Нативні `alert()`/`confirm()`/`prompt()` — помилка ESLint (`no-alert`).
- Ролі: `grantAdmin` / `revokeAdmin` (`src/actions/admin-roles.ts`) — поверхня `/admin/people`. `User.isAdmin` пишуть лише `promoteToAdmin` / `demoteFromAdmin` у `src/data/users.ts`; `demoteFromAdmin` під `SELECT … FOR UPDATE` відмовляється зняти останнього адміна. «Користувач, що входив» = має рядок `account` (`listAuthenticatedUsers`).
- Турнірна таблиця й місця плейофа обчислюються при читанні, ніколи не зберігаються в БД.
- `Tournament.state` змінюється лише через Server Action `transitionTournament`, не присвоєнням.
- У стані `COMPLETED` результати матчів і розклад заморожені: `checkCanEditResults(state)` (`src/domain/tournamentState.ts`) гейтить `enter`/`edit`/`removeMatchResult` і `scheduleMatch` (Story 4.5 / FR-7).
- Інтерфейс лише українською, без i18n-бібліотеки; час зберігається в UTC, показується в `Europe/Kyiv`; ID — cuid.
- Планові документи — українською; ідентифікатори в коді — англійською.
- Публічний каркас (Story 1.8): вордмарк + `DisciplineNav` (`src/components/discipline-nav.tsx`) у шапці `src/app/layout.tsx`; розділи `/classic` · `/beach` · `/archive` — незалежні дерева маршрутів; `/` редіректить на `/classic`. `EmptyState` (`src/components/empty-state.tsx`) формалізовано в Story 2.2 (`action` / `headingLevel` слоти, копірайт у `src/lib/empty-states.ts`).

## Known pitfalls

- **Увесь тулчейн встановлено** (Node 24, pnpm 11, uv 0.12, Python 3.11, git). Раніше «немає Python/uv» — застаріло.
- **PowerShell-інструмент**: PATH налаштовано через `.claude/settings.local.json` (`env.PATH`, gitignored, машинно-специфічні шляхи для користувача `night`) — `node`/`pnpm`/`npm`/`uv`/`python`/`git` резолвляться без дій. BMAD-скрипти (`resolve_customization.py`, `render_skill.py`) і `bmad-build` — через `uv run` у PowerShell. **Інший розробник/машина**: відтворити `.claude/settings.local.json` зі своїми шляхами (`(Get-Command node).Source` тощо).
- **Bash-інструмент**: не підхоплює той `env.PATH` і чистить Windows-шляхи при POSIX-конверсії — `node`/`pnpm`/`uv` там недоступні; `git` є (`/mingw64/bin/git`, або повний шлях `C:\Program Files\Git\cmd\git.exe`). Для node/pnpm/uv/BMAD — використовувати PowerShell-інструмент.
- `gh` (GitHub CLI) **не встановлено** — `winget install GitHub.cli` за потреби. Операції з git — через `git` напряму.

<!-- /bmad:context -->

<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->

<!-- bmad:manual -->
<!-- Notes below are maintained by hand, outside the bmad-managed block above. -->

## Stack status (Story 1.1)

- Scaffolded: Next.js 16 (App Router, `src/`, Turbopack default), React 19.2, TypeScript 5, Tailwind CSS **v4** (CSS-first — theme in `src/app/globals.css` via `@theme`, no `tailwind.config.js`), ESLint 9 flat config, shadcn/ui (Tailwind v4 mode), Prisma **7** (`prisma-client` generator, output `src/generated/prisma`, `prisma7.config.ts` holds the connection URL — not `schema.prisma`).
- Package manager: **pnpm** (installed globally via `npm i -g pnpm`; `corepack enable` fails on this machine — Node lives in `C:\Program Files\nodejs`, not user-writable). `packageManager` field pins the version.
- Node: pinned to 24.x via `package.json` `engines` + `.nvmrc`.
- Commands: `pnpm dev` (Turbopack), `pnpm build`, `pnpm lint`, `pnpm typecheck`, `pnpm seed`.
- Prisma 7 driver adapter wired (Story 1.4): `@prisma/adapter-pg` + `pg` over the pooled `DATABASE_URL`; the shared `PrismaClient` is `src/data/client.ts` (`export const db`), imported from `@/generated/prisma/client`. Migrations use the direct URL (`DATABASE_URL_UNPOOLED` / `DIRECT_URL`) via `prisma7.config.ts`. `build` = `prisma generate && node scripts/migrate-deploy.mjs && next build` (the wrapper runs `migrate deploy` only when `VERCEL_ENV` is `production` or unset). `prisma/seed.mts` + `prisma7.config.ts` are the only sanctioned importers of the generated client outside `src/data/` (build/CLI scripts, not linted).
- **Neon `dev` branch (set up 2026-09-04).** Project `twilight-dust-91359102` now has two branches: `main` (prod, `br-young-mountain-b2g7kj2g`) and `dev` (`br-winter-term-b2moucuf`, forked from `main` after migration `20260904200627_team_name_key` — same schema + whatever data `main` had at fork time). Local `.env.local`'s `DATABASE_URL`/`DATABASE_URL_UNPOOLED` now point at **`dev`**, not `main` — `pnpm dev`, `pnpm build` (local), and any Prisma CLI command run from a local shell hit `dev`. **Vercel's own env vars are untouched** and still point at `main`; the deployed app and its `migrate deploy` (via `scripts/migrate-deploy.mjs`) still target production exactly as before. Reset `dev` back to a clean fork of `main` any time: `npx neonctl branches reset dev --project-id twilight-dust-91359102 --org-id org-calm-waterfall-58712226 --parent`. Regenerate its connection strings (they rotate the password) with `npx neonctl connection-string dev --project-id twilight-dust-91359102 --org-id org-calm-waterfall-58712226 [--pooled]`. For a one-off check against real prod data, pull it separately — `npx vercel env pull .env.production.local --environment=production` — rather than repointing this file.
- **`prisma migrate dev` is still non-interactive-blocked in this tool regardless of which branch it targets** — the block is Prisma detecting no TTY, not a prod-safety check, so the `migrate diff --script` → hand-write `migration.sql` → `migrate deploy` fallback (below) is unchanged. What the `dev` branch actually buys: a safe place to `migrate reset` for from-empty replay testing, and a place a human running `migrate dev` **directly in their own interactive terminal** (outside this tool) now lands on instead of `main`.
- Auth (Story 1.5): **Better Auth 1.7.x**, Google provider only (`emailAndPassword: { enabled: false }`). `src/auth/auth.ts` (server instance; throws in prod if auth env vars are missing) · `src/lib/auth-client.ts` (`createAuthClient` + `inferAdditionalFields`, browser) · `src/app/api/auth/[...all]/route.ts` (`toNextJsHandler`, `runtime = "nodejs"`). Tables `user`/`session`/`account`/`verification` (`@@map` lowercase; models stay `PascalCase`). `User` table renamed `User` → `user`; `googleSub` dropped (OAuth identity lives in `account`; `account` is keyed `@@unique([issuer, accountId])`). `isAdmin` is a Better Auth `additionalFields` entry with `input: false` — on `session.user`, set only by the Story 1.7 action. Env: `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `BETTER_AUTH_SECRET` (≥32 chars), `BETTER_AUTH_URL`. Google Cloud redirect URI: `<BETTER_AUTH_URL>/api/auth/callback/google`. **All table names are lowercase via `@@map` going forward** (Better Auth convention; Postgres-friendly).
- `@better-auth/cli` lags the `better-auth` runtime — after `@better-auth/cli generate`, reconcile the schema against `@better-auth/core/db/get-tables.mjs` (the runtime's own table defs), not just the CLI output.
- DB conformance check: `pnpm exec prisma migrate diff --from-config-datasource --to-schema prisma/schema.prisma --script` must print "empty migration"; `pnpm exec tsx scripts/db-check.mts` prints row counts.
- Epic 2 schema (Story 2.1, migrations `20260903174727_tournament_schema` + `20260903204803_tournament_schema_constraints`): `tournament` / `team` / `tournament_entry` / `player` + enums `Discipline` / `TournamentType` / `TournamentState` / `ScoringPreset`. `Team.name` is `@unique` (case/whitespace-sensitive — Story 2.6 owns normalization); `TournamentEntry` is `@@unique([tournamentId, teamId])`; `Player` needs only `fullName`, the rest are optional free-text `String?`. Timestamps are `@db.Timestamptz(3)`. CHECK: `tournament.year` 2000–2100, `teamCount` > 0, `rounds` > 0 (added by raw SQL in the migration — Prisma 7 doesn't model CHECK; `migrate diff` ignores them). Deletes: tournament→entries→players cascade; a `team` with entries is `onDelete: Restrict`. `Group` landed in Story 2.4 (below); `GroupSlot` / `Match` / `SetScore` come in Epic 3.
- Enum migration hygiene: `ALTER TYPE … ADD VALUE` must be in **its own migration** — a new enum label cannot be used in the same transaction that adds it. Postgres cannot `DROP` an enum value that any row still uses (backfill first, drop in a later migration).
- Tournament state machine (Story 2.3): `src/domain/tournamentState.ts` — `TRANSITIONS` (forward-only `DRAFT → GROUP_STAGE → PLAYOFF → COMPLETED`), `canTransition` (edge-only, for the view), `checkTransition(from, to, ctx)` (edge + precondition). `DRAFT → GROUP_STAGE` precondition (заявки == `teamCount`) активна; `→ PLAYOFF` / `→ COMPLETED` — fail-closed предикати, доки не змодельовано результати матчів. Server Action `transitionTournament` (`src/actions/tournaments.ts`) — єдиний шлях зміни `state`; `setTournamentState` у `src/data/tournaments.ts` — єдиний письменник. Vitest додано з цим модулем. Перший реальний виклик переходу — у Epic 3 (жеребкування).
- Створення турніру (Story 2.4, міграція `20260904160000_tournament_group_and_natural_key`): `Group`-модель (`tournamentId @unique` — одна група на турнір у v1, `onDelete: Cascade`; слоти/матчі — Epic 3), `Tournament @@unique([discipline, type, year, name])`. `src/domain/tournamentForm.ts` — `allowedTournamentTypes` + межі + `validateNewTournament` (Vitest). `src/data/tournaments.ts` — `createTournamentRecord` (єдиний творець `Tournament` + `Group`, `state` за замовч. `DRAFT`), `isUniqueViolation` (P2002). Server Action `createTournament` (`useActionState`-shape, `redirect` на `/admin/tournaments/[id]`, `P2002` → «дублікат»). Форма `src/components/tournament-form.tsx` (native `<select>` для type/preset; повністю контрольований стан — `@base-ui/react`'s `Input` ігнорує змінений `defaultValue`, тож обхід ресету `<form action>` React 19 зроблено через `useState`/`value`/`onChange`, а не echo назад через action-стейт). Сторінки `/admin/tournaments/new` + `[id]` (Story 2.5 замінила заглушку на форму редагування). Мітки — `src/lib/tournament-labels.ts`. Перший санкціонований `data → domain` та `view → domain` (type/const) імпорт.
- Редагування й видалення турніру (Story 2.5): `src/data/tournaments.ts` — `listTournamentsForAdmin`, `updateTournamentRecord` (другий письменник `Tournament`, ніколи `state`/`discipline`), `deleteTournamentRecord` (каскад через FK). Server Actions `updateTournament` (`useActionState`-shape, `teamCount`/`rounds` підміняються поточними БД-значеннями поза `DRAFT` через доменну `resolveGroupStageFields` — сервер, не лише форма, забороняє їх зміну) та `deleteTournament` (`ActionResult`, без стану-обмеження на видалення — рішення задокументоване в самій історії). `src/components/tournament-form.tsx` — `mode: "create" | "edit"` (дискримінована спілка пропсів); edit-режим замикає `locked`-поля (`disabled` + підпис) і success-тост через відстеження заднього фронту `pending`, не ідентичності `state`. `src/components/tournament-actions.tsx` (NEW) — `DeleteTournamentButton` (`ConfirmDialog`). `src/app/admin/tournaments/page.tsx` (NEW) — список турнірів (закриває прогалину «нема способу знайти турнір для редагування» з рев'ю Story 2.4).
- Довідник команд (Story 2.6, міграція `20260904200627_team_name_key`): `Team.name` втратив власний `@unique`; `Team.nameKey` (case-fold `name`) — новий якір унікальності, обчислюється разом з `name` у `validateNewTeam` (`src/domain/teamForm.ts`, разом з `normalizeTeamName`/`teamNameKey`). `isUniqueViolation`/`isRecordNotFound` винесено з `src/data/tournaments.ts` у спільний `src/data/errors.ts` (третій споживач — `src/data/teams.ts`, як передбачило рев'ю Story 2.5). `src/data/teams.ts` — `listTeams`, `createTeamRecord`, `TEAM_NAME_KEY_INDEX`. Server Action `createTeam` (`useActionState`-shape, без redirect — залишається на `/admin/teams`, ревалідує список). `/admin/teams` — одна сторінка (форма + список), без окремого `/new` (буквальне читання AC). Немає редагування/видалення команд у v1 (поза FR-8).
- Заявка/зняття команди з турніру (Story 2.7): `src/domain/teamEnrollment.ts` — `checkCanEnroll(state, currentEntryCount, teamCount)` (DRAFT + ліміт місць) і `checkCanRemoveEntry(state)` (DRAFT-only), обидві чисті й тестовані — той самий патерн, що `resolveGroupStageFields` (Story 2.5), проактивно закриває клас проблеми з двох попередніх рев'ю. `countTournamentEntries` переїхав з `src/data/tournaments.ts` у новий `src/data/entries.ts` (той самий принцип релокації, що й `errors.ts` у Story 2.6); там же — `listEntriesForTournament`, `createEntry`, `deleteEntry`, `TOURNAMENT_ENTRY_NATURAL_KEY_INDEX`. Server Actions `enrollTeam`/`removeTeamEntry` (`src/actions/entries.ts`) — `ActionResult`-shape, як `admin-roles.ts`. `src/components/team-enrollment.tsx` — секція «Команди» на `/admin/tournaments/[id]` (новий роут не додавався); скасування заявки теж обмежено DRAFT, хоч буквально AC каже це лише про заявлення нової команди — рішення задокументоване в story-файлі.
- Склад команди (Story 2.8): новий роут `/admin/tournaments/[id]/entries/[entryId]` (Server Component, `getEntryForAdmin` → `notFound()`; вимагав `pnpm build` перед `pnpm typecheck`, щоб перегенерувати `.next/types` для нового вкладеного динамічного маршруту). `src/domain/playerForm.ts` — `FULL_NAME_MAX`/`FREE_TEXT_MAX` + `validatePlayer` (Vitest); порожнє опціональне поле після trim стає `null`, не `""` (відповідає `Player`-моделі `String?`). `entries.ts`'s Story 2.7 рев'ю знайшло крос-турнірний баг видалення (`deleteEntry` за самим лише `entryId`), виправлений там-таки (`deleteEntry(tournamentId, entryId)`) ще до цієї історії — той самий принцип («скановано за парою ключів разом, ніколи за дочірнім id окремо») застосовано в `src/data/players.ts` з нуля: `updatePlayer`/`deletePlayer` скановані за `(entryId, playerId)` через `updateMany`/`deleteMany` → `{count}`. Server Actions `src/actions/players.ts` (`addPlayer`/`editPlayer`/`removePlayer`) без обмеження за `Tournament.state` (на відміну від `entries.ts` — AC не обмежує редагування складу станом турніру). `src/components/player-form.tsx` (`mode: "create" | "edit"`, та сама дискримінована спілка пропсів, що й Story 2.5 форма) і `roster.tsx`. Дві нові ESLint-правила зустрілись уперше: `react-hooks/set-state-in-effect` (заборонив пряме `setState(literal)` в ефекті — фікс через updater, що читає попередній стан) і `react-hooks/refs` (заборонив запис у `ref.current` під час рендеру — фікс переносом виклику в ефект). Нема обмеження розміру складу, нема перевірки на дублікат ПІБ (AC — свідома відсутність), нема публічного читання складу (рішення Story 2.9).
- Публічна сторінка турніру та вкладка «Команди» (Story 2.9): перші публічні (без перевірки ролі) читання в кодовій базі — `getPublicTournament`/`listPublicTournaments` (`src/data/tournaments.ts`, AD-7: `state != DRAFT` **і** `discipline = CLASSIC` завжди) і `getEntryByTeam(tournamentId, teamId)` (`src/data/entries.ts`, той самий принцип «скановано за парою ключів», що й `getEntryForAdmin`, але свідомо без фільтра видимості — рішення про видимість лишається на рівні сторінки). Нові маршрути `/classic/[tournament]` і `/classic/[tournament]/teams/[team]`; `/classic` перестав бути постійною заглушкою (Story 1.8) і тепер показує реальний список. **Санкціонований `view → auth` край, новий цього разу**: обидві сторінки (через спільний `src/app/classic/_lib/resolve-tournament.ts`) викликають `getSessionUser()` (`src/auth/requireAdmin.ts`) як fallback, коли публічне читання повертає `null` — якщо відвідувач адмін, показує чернетку турніру для прев'ю (epics.md і EXPERIENCE.md обидва незалежно кажуть «404 для **не-адміна**», а не для всіх); лінт-конфіг це вже дозволяв (`src/components/**` без `@/auth`, `src/app/**` — без обмежень). **Рев'ю коду знайшло (усі 4 шари незалежно) критичний баг**: fallback-функція `getTournamentForAdmin` не має фільтра `discipline`, тож без додаткової перевірки адмін міг би побачити BEACH-турнір через `/classic`; `resolveTournament` тепер сам перевіряє `discipline === "CLASSIC"` після fallback-виклику. Нові компоненти `status-badge.tsx` (перший публічний статус-пігулка, стилі за `DESIGN.md`) і `public-roster.tsx` (read-only, окремий від адмінського `roster.tsx`). Вкладки Розклад/Таблиця/Плейоф — інертні заглушки inline-розміткою, без окремого перевикористовуваного компонента (передчасна абстракція для однієї реальної вкладки — відкладено до Epic 3). Немає slug — `[tournament]`/`[team]` лишаються сирими `cuid` (закриває відкритий пункт з рев'ю Story 2.1). Перша story з часів 2.1 без нового `src/domain`-модуля.
- Чистий двигун групового етапу (Story 3.1, перша story Epic 3): чотири нові `src/domain`-модулі, жодного коду поза `src/domain` — `scoring.ts` (`matchPoints`: `CLASSIC` 3/0 за 3:0 чи 3:1, 2/1 за 3:2; `CUSTOM` 1 очко за партію; `computeStandings` — агрегація з нуля щоразу, AD-4), `tiebreak.ts` (`orderStandings` — ланцюжок очки → особиста зустріч через міні-таблицю (лише серед тих команд, що досі рівні) → виграні партії → назва, з прапорцем `needsManualSeed`), `schedule.ts` (`generateSchedule` — коловий метод, «пропуск» для непарної кількості команд, кілька «кіл» повторюють той самий набір пар без зміни господар/гість — свідоме рішення), `validation.ts` (`targetScore`/`validateSetScore`/`validateMatchScore` — Ветеранський завжди до 15, вирішальна 5-та в Класичному завжди 15, різниця в 2 очки застосована до **обох** пресетів, хоча PRD прямо каже це лише для Класичного). «Відкрите питання №5» з PRD насправді вже вирішене в §11 PRD, не є справді відкритим. 99 тестів одразу після реалізації, 103 після фіксів рев'ю (`pnpm test`) — найбільше приросту тестів за одну story.
- Схема групового етапу (Story 3.2, міграції `20260905125839_group_stage_schema` + `20260905161412_group_stage_schema_constraints`): `MatchStage` (`GROUP`/`SEMIFINAL`/`THIRD_PLACE`/`FINAL`), `GroupSlot` (міст членства в групі — окремо від `TournamentEntry`, на випадок майбутнього мультигрупового формату), `Match` (`homeEntryId`/`awayEntryId` **навмисно nullable** — буквальний запис AC у epics.md цього не показує, але AD-5 вимагає nullable для матчів плейофу з ще невідомими учасниками, Epic 4; для `GROUP`-матчів обидва поля завжди заповнені з нуля), `SetScore` (єдине джерело результату, AD-4 — жоден рядок `Match` не зберігає переможця/рахунок). П'ять raw-SQL CHECK за патерном Story 2.4 (три від Task 2: узгодженість `stage`/`groupId`, команда не може грати сама з собою, невід'ємні очки партії; два від рев'ю: `GROUP`-матч має мати обидва `entryId`, `setNo` в межах 1–5) + індекси на всіх cascade-FK колонках (`Match.groupId`/`homeEntryId`/`awayEntryId`, `GroupSlot.entryId`) та `GroupSlot.updatedAt` для узгодженості з рештою моделей. `src/data/matches.ts`'s `getStandings(tournamentId)` — перший **value**-виклик `data → domain` (усі попередні були лише type-only): `GroupSlot` → `computeStandings` → `orderStandings` (обидва з Story 3.1, без змін).
- Жеребкування (Story 3.3): перший реальний виклик доменного двигуна Story 3.1 та запис у схему Story 3.2. `setTournamentState(id, state, client?)` (`src/data/tournaments.ts`) отримав необов'язковий третій параметр — `Prisma.TransactionClient`, за замовч. спільний `db` — щоб брати участь у транзакції жеребкування без порушення AD-8 (єдиний письменник лишається той самий); `getTournamentForAdmin` тепер включає `group: { id }`. `src/data/draw.ts` (NEW) — `saveDraw(tournamentId, groupId, entryIds, pairings)`: один `db.$transaction` — заповнює `GroupSlot`, створює `GROUP`-матчі, викликає `setTournamentState(..., tx)`; без власної валідації (її вже зробив виклик `checkTransition` у дії). `src/actions/draw.ts` (NEW) — `drawTournament(tournamentId)`: **окрема** Server Action, а не вкладений виклик `transitionTournament` (жеребкування робить доменну роботу — розсадка, генерація календаря, — якої немає в тій узагальненій дії); напряму перевикористовує `checkTransition` (`src/domain/tournamentState`, той самий предикат, що вже давав готове повідомлення про нестачу заявок) → `generateSchedule` (`src/domain/schedule`) → `saveDraw`. `DrawTournamentButton` (`src/components/tournament-actions.tsx`) — той самий патерн `useTransition` + пряма дія, що `team-enrollment.tsx`'s `enroll()`; без `ConfirmDialog` (нема в закритому переліку `EXPERIENCE.md`); disabled+підпис через `checkTransition` на клієнті (`view → domain` край, той самий, що вже встановив `checkCanEnroll`). `round`/`tour` з `generateSchedule` навмисно не зберігаються в `Match` — потрібні лише для коректності самого алгоритму жеребкування. **Рев'ю коду знайшло і виправило**: (1) `entryIds` доходили до `generateSchedule` в алфавітному порядку (як їх повертає `listEntriesForTournament`) без жодного перемішування — фіксований якір `circleMethodTours` робив реальні пари команд у турах повністю детермінованими за назвою команди, а не випадковими; тепер `drawTournament` перемішує `entryIds` через новоекспортовану `defaultShuffle` (`src/domain/schedule.ts`) перед `generateSchedule`/`saveDraw`. (2) Секція «Жеребкування» рендерилась завжди, незалежно від стану — після проведення жеребкування кнопка лишалась назавжди з незрозумілим повідомленням `checkTransition` («Неможливий перехід зі стану «Груповий етап» до «Груповий етап»»); тепер секція показується лише при `state === "DRAFT"`. (3) `drawTournament` не ревалідував `/admin/tournaments` — список турнірів показував застарілий стан «Чернетка»; додано `revalidatePath("/admin/tournaments")`. (4) `saveDraw`'s гарантія атомарності транзакції була задокументована, але не перевірена жодним тестом — `scripts/verify-draw.mts` розширено перевіркою, що повторний виклик `saveDraw` на вже розжеребкованій групі падає і не лишає часткового стану (кількість `GroupSlot`/`Match` і `Tournament.state` незмінні).
- Пережеребкування (Story 3.4): повторний запуск того самого пайплайна Story 3.3, без кроку розсадки. `src/domain/redraw.ts` (NEW) — `checkCanRedraw(state, hasResults)`, той самий патерн, що `checkCanEnroll`/`checkCanRemoveEntry` (Story 2.7) — **не** `checkTransition`, бо `Tournament.state` під час пережеребкування не змінюється (лишається `GROUP_STAGE`). `src/data/matches.ts` отримав `hasAnyGroupResult(tournamentId)` — єдине джерело для «результатів ще нема». `src/data/draw.ts` отримав `listGroupEntryIds(groupId)` (читає `GroupSlot`, а не `TournamentEntry` — склад групи при пережеребкуванні не змінюється) і `saveRedraw(tournamentId, groupId, pairings)` — один `db.$transaction`: видаляє всі `GROUP`-матчі турніру й створює нові з тих самих (перемішаних через `defaultShuffle`) заявок; ніколи не чіпає `GroupSlot` чи `Tournament.state`. `src/actions/draw.ts` отримав `redrawTournament(tournamentId)` поряд з `drawTournament` — та сама файлова структура (одна доменна область). `RedrawTournamentButton` (`src/components/tournament-actions.tsx`), на відміну від `DrawTournamentButton`, **має** `ConfirmDialog` — рішення явно назване в самому AC epics.md («тисну «Пережеребкувати» **й підтверджую**»), хоча закритий перелік `EXPERIENCE.md` пережеребкування не називає. Секція рендериться лише при `state === "GROUP_STAGE"`; після першого результату кнопка лишається видимою, але заблокованою (`checkCanRedraw`) — PRD FR-12 прямо каже «заблоковане», не «зникає». **Рев'ю коду знайшло і виправило**: `hasAnyGroupResult` перевірявся до початку транзакції `saveRedraw`, тож результат, внесений у вузькому вікні між перевіркою й записом, міг тихо загубитися через каскадне видалення `Match`; тепер `hasAnyGroupResult` приймає необов'язковий параметр транзакційного клієнта (той самий патерн, що `setTournamentState`), і `saveRedraw` перевіряє його ще раз усередині власної транзакції, відмовляючись писати, якщо результат уже з'явився.
- Розклад матчів (Story 3.5): **без міграції** — `Match.scheduledAt` / `Match.venueText` вже існують зі Story 3.2. `src/domain/matchSchedule.ts` (NEW) — перша в кодовій базі робота з датами/часом для користувача: `parseKyivDateTimeLocal` (рядок `datetime-local` як київський настінний час → UTC `Date`, з одним переперевіренням зсуву на межі DST; порожньо → `null`), `toKyivDateTimeLocalValue` (зворотнє — сідинг інпута), `formatKyivDateTime` (`uk-UA` для показу), `kyivOffsetMinutes` (зсув Europe/Kyiv з `Intl`, **без tz-бібліотеки** — NFR-4), `validateMatchSchedule` + `VENUE_TEXT_MAX`. `src/data/matches.ts` — `listGroupMatchesForTournament` (спільний read для публічної вкладки й адмінки, `orderBy` за `scheduledAt` з `nulls: "last"`, потім `createdAt`; visibility-agnostic — як `getEntryByTeam`) і `updateMatchSchedule(tournamentId, matchId, input)` (`updateMany` за парою `(tournamentId, matchId)` + `stage: "GROUP"` → `{count}`; пише лише `scheduledAt`/`venueText`, ніколи `SetScore` — FR-13 «не впливає на результат»). `src/actions/matches.ts` (NEW) — `scheduleMatch` (форма-стейт як `players.ts`, без обмеження за `Tournament.state` — FR-13 «будь-якого Матчу», і Match не існує до жеребкування). `?tab=`-роутинг на `/classic/[tournament]` **нарешті реалізовано** (відкладено зі Story 2.9 «до першої реальної вкладки»): `src/components/tournament-tabs.tsx` (серверний, chip-и як `<Link href="?tab=…">`, `normalizeTournamentTab`, вкладка «Плейоф» ховається до стану `PLAYOFF`+). Нова адмін-сторінка `/admin/tournaments/[id]/schedule` (потребувала `pnpm build` перед `pnpm typecheck` для нового вкладеного роуту); `src/components/match-schedule.tsx` (per-row `useActionState`-форма) + `src/components/public-schedule.tsx` (read-only, як `public-roster.tsx`). `drawTournament`/`redrawTournament` тепер також `revalidatePath(`/classic/${id}`)` (закрито відкладений пункт). Підсумковий рахунок матчу («3:1») рахується інлайн-редьюсером `setSummary`, продубльованим у двох сторінках — свідомо: канонічний хелпер належить Story 3.6 (у `deferred-work.md`). Перша story з часів 3.1 з новим `src/domain`-модулем.
- Внести результат матчу (Story 3.6): **без міграції** — `SetScore` зі Story 3.2. Тільки внесення (перший раз); виправлення/видалення — Story 3.7. `src/domain/scoring.ts` отримав `matchSetSummary(sets): {home, away}` — канонічний хелпер підсумку «3:1» (на тій самій `homeWonSet`-звірці, що й `countSetsWon`); **закрито відкладений пункт Story 3.5** — обидва inline-редьюсери в сторінках замінено. `src/domain/validation.ts` (Story 3.1) — **єдиний валідатор** (AC вимагає прямо); нічого не переписуємо. `src/data/matches.ts` — `getMatchForResult(tournamentId, matchId)` (read екрана матчу, скоуп за парою) і `createMatchResult(...)` (один `$transaction`: матч існує + `GROUP` + 0 партій → `createMany`; `P2002` за `SET_SCORE_NATURAL_KEY_INDEX` → `{ok:false, reason:"exists"}`, не кидає). `src/actions/matches.ts` — `enterMatchResult` (`MatchResultFormState` з `setErrors`; повідомлення `validateMatchScore` з префіксом «Партія N:» мапиться під рядок партії). Новий роут `/admin/tournaments/[id]/matches/[matchId]` (4 сегменти — `pnpm build` перед `pnpm typecheck`); `src/components/match-result-form.tsx` (Score input, UX-DR8 — 3 поля для CUSTOM / до 5 для CLASSIC, live-підсумок через `matchSetSummary`, `tabular-nums`); лінк «Внести результат» / «Результат: X:Y» з галочкою `text-success` на рядках `match-schedule.tsx`. **Видимої таблиці немає** — «Таблиця» лишається схованою до Story 3.8 (рішення користувача — option A); `getStandings` уже рахує правильно, `revalidatePath` спрацьовує. `--color-success` токен уже є в `globals.css` (`text-success` працює).
- Виправити/видалити результат (Story 3.7): завершує пару зі Story 3.6, **без міграції / без нового домену / без нового роуту**. `src/data/matches.ts` — `replaceMatchResult` (транзакція delete-then-insert, без `_count`-перевірки; `not_found` через спільний `isMissingMatch` = `P2025`/`P2003`) і `deleteMatchResult` (`setScore.deleteMany` за вкладеним `match`-фільтром → `{count}`). `src/actions/matches.ts` — `editMatchResult` (як `enterMatchResult`, але вимагає наявний результат; спільний хелпер `parseAndValidate` = parse + `validateMatchScore` + мапінг «Партія N:»; спільний `revalidateMatchSurfaces` для 4 шляхів) і `removeMatchResult` (`ActionResult`, шаблон `removePlayer`). `MatchResultForm` отримав `mode: "create" | "edit"` (дискр. спілка пропсів, патерн `player-form`). Новий `src/components/match-result-panel.tsx` (клієнтський, `editing`-стан як `roster.tsx`): read-only + «Виправити» + `ConfirmDialog` «Видалити результат матчу? Таблиця групи перерахується.» (UX-DR10, `destructive`). Без `Tournament.state`-гейту (блокування в `COMPLETED` — FR-7 / Story 4.5). Плейоф-сітка з AC — Epic 4; `revalidatePath` уже покриває майбутню вкладку, коду немає.
- Публічна турнірна таблиця (Story 3.8, завершує Epic 3): **story рендерингу — без міграції / без домену / без роуту**. Двигун готовий: `getStandings` (Story 3.2) уже впорядковує, `enterMatchResult`/`editMatchResult`/`removeMatchResult` уже роблять `revalidatePath` публічної сторінки. Зміни: `getStandings` повертає `StandingsView` (`OrderedStandingsRow & { teamName }` — карта імен уже будувалась усередині); `src/components/standings-table.tsx` (NEW, серверний, read-only, як `public-schedule.tsx`) — семантична розмітка UX-DR5 (`<th scope>`, `<caption sr-only>`, `<abbr>`-заголовки, `tabular-nums`, без зебри, `overflow-x-auto role=region aria-label`), позиції 1–4 `font-bold text-primary` + `title` + `sr-only` + легенда (не лише колір — UX-DR13); `tournament-tabs.tsx` — порядок вкладок за DESIGN §176 (**Таблиця · Розклад · Команди · Плейоф**), прибрано `showStandings` (chip завжди), `normalizeTournamentTab` → `TournamentTabKey | null`; `classic/[tournament]/page.tsx` — **дефолтна вкладка `standings` у `GROUP_STAGE`+** (`teams` для DRAFT-прев'ю адміна). Порожньо: «команди з нулями + рядок Результатів поки немає»; до жеребкування — `GROUP_NOT_DRAWN`. **Закрито 2 відкладені пункти** (chip «Таблиця» схований + порядок/дефолт — рев'ю Story 3.5). `standings-table.tsx` перевикористає Story 4.7 (архів).
- Чистий двигун плейофа (Story 4.1, перша story Epic 4): **лише `src/domain/bracket.ts` (+ `bracket.test.ts`)**, жодного коду поза `src/domain` — той самий формат, що Story 3.1. **Без міграції** — enum `MatchStage` (`GROUP|SEMIFINAL|THIRD_PLACE|FINAL`) і nullable `Match.homeEntryId/awayEntryId` уже є зі Story 3.2 (створені саме для цього). Три чисті функції: `seedPlayoff(standings)` — бере топ-`PLAYOFF_QUALIFIERS` рядків упорядкованої таблиці (`OrderedStandingsRow[]` з `tiebreak.ts`, index 0 = посів 1), пари `SF1` = посів 1 (господар) vs 4, `SF2` = посів 2 vs 3 (вищий посів — господар, свідомо-стабільний вибір), фінал/матч за 3-тє повертаються `AWAITING`; `needsManualSeed` = OR по топ-4 (сигнал, що межа 4/5 вирішена за назвою — half закритого пункту рев'ю 3.8); `< 4` рядків → `RangeError`. `advanceBracket(matches)` — **єдине місце** виведення учасників наступного раунду (і на запис, і на рендер — AD-5): фінал отримує переможців півфіналів, матч за 3-тє — тих, хто програв, **доки в парі немає власного `SetScore`**; після цього пара `PLAYED` і заморожена (пізніше виправлення півфіналу не переписує зіграний матч). Статуси `AWAITING`/`READY`/`PLAYED` (годують `bracket-pair` vs `bracket-pair-tbd`). Приймає 2–4 матчі, ключ — `slot` (`SF1|SF2|THIRD_PLACE|FINAL`), не порядок масиву; переможець завжди з `matchSetSummary` (нема колонки-переможця — AD-4). `playoffPlacements(matches)` — місця 1–2 з фіналу, 3–4 з матчу за 3-тє (`null` доки не зіграно), ніколи не зберігається; додано проактивно, споживає Story 4.2/4.4. `bracket.ts` імпортує лише `@/domain/scoring` і `@/domain/tiebreak`. `pnpm test` 155/155 (+20). Дроти до БД/дій/UI — Stories 4.2–4.6; `tournamentState.ts` `PLAYOFF`/`COMPLETED` стаби не чіпалися.
- Сформувати плейоф (Story 4.2): дріт `seedPlayoff` → БД, аналог жеребкування (Story 3.3). **Міграція `20260907120000_match_playoff_slot`** (ручний SQL, як 2.4/3.2): `enum MatchSlot { SF1 SF2 THIRD_PLACE FINAL }` (ті самі рядки, що `BracketSlot`) + `Match.slot MatchSlot?` + CHECK `match_slot_stage_check` = `("stage" = 'GROUP') = ("slot" IS NULL)` — **розв'язує** відкладений пункт «два `SEMIFINAL`-рядки однакові»; три попередні verify-скрипти тепер ставлять `slot` на свої SEMIFINAL-фікстури. `src/data/matches.ts` — `allGroupMatchesPlayed(tournamentId, client?)` (precondition FR-19, на відміну від `hasAnyGroupResult` — «всі», не «хоч один»). `src/data/playoff.ts` (NEW) — `savePlayoffFormation(tournamentId, bracket)`: один `db.$transaction` — `SELECT … FOR UPDATE` на рядку турніру (серіалізує паралельні формування, як `GroupSlot`-унік для `saveDraw`), потім дві перевірки що **повертають результат, не кидають** (`{ ok:false, reason: "already_formed" | "group_incomplete" }` — обидві нормальні гонки), потім 2 `SEMIFINAL`-`Match` (`groupId: null`, `slot`) + `setTournamentState(..., "PLAYOFF", tx)`. Мапінг `bracket.semifinals → рядки` живе тут (одне місце). `src/actions/playoff.ts` (NEW) — `formPlayoff(tournamentId)`: `requireAdmin` → `getTournamentForAdmin` → `checkTransition(state, "PLAYOFF", { allGroupMatchesPlayed })` (єдиний виклик покриває і ребро, і precondition) → `getStandings` → `seedPlayoff` → `savePlayoffFormation` (гонка → `PRECONDITION_FAILED`) → `revalidatePath` (вкл. `/admin/tournaments` + публічну сторінку — зʼявляється вкладка «Плейоф»); повертає `needsManualSeed` (true = порядок топ-4 вирішено за назвою команди). `tournamentState.ts` **не чіпався** — `PLAYOFF`-предикат уже приймав `ctx.allGroupMatchesPlayed`. `FormPlayoffButton` (`tournament-actions.tsx`) — як `DrawTournamentButton`, **без `ConfirmDialog`**, підпис `PRECONDITION_FAILED` → «Доступно коли всі матчі груп зіграно»; при успіху + `needsManualSeed` — `notify.warning` (новий рівень у `notify`). Секція `GROUP_STAGE` на `[id]/page.tsx` поряд із redraw. Мультигруповість (AC 4) — структурно неможлива (`Group.tournamentId @unique`), гілки нема. `scripts/verify-generate-playoff.mts` (NEW). Без нового Vitest (161). Фінал/матч за 3-тє, автопросування — Story 4.3; публічна сітка — 4.6; вхід результатів півфіналів — 4.4.
- Результати плейофа й фінальні місця (Story 4.4): дві останні частини обробки результатів плейофа (ввід результатів уже ввімкнено Story 4.3). (1) `playoffPlacements` (Story 4.1, досі не використовувалась) вбудовано в `getPlayoffBracket` → `PlayoffBracketView.placements` (місця 1–4 з іменами команд, обчислюються при читанні з тих самих рядків, `null` доки вирішальний матч не зіграно — AD-4); показ **лише в адмін-секції «Плейоф»** сторінки розкладу (новий `src/components/playoff-placements.tsx`, «Місця», порядковий підпис — не колір). (2) Нова чиста предикат-функція `checkCanEditSemifinalResult(matches)` (`src/domain/bracket.ts`, форма `checkCanRedraw`) блокує правку/видалення результату півфіналу, коли Фінал **або** Матч за 3-тє має власний `SetScore` («команда у двох місцях» — гейт, який `advanceBracket` явно не забезпечує); enforced у `editMatchResult` / `removeMatchResult` (`PRECONDITION_FAILED`) через новий лінивий `readPlayoffMatchStates`. `enterMatchResult` не чіпається. Без міграції, без нового роуту, без нового домен-модуля, `Tournament.state` не чіпається (це 4.5). Публічна сітка — 4.6; архів місць — 4.7. `pnpm test` 167 (+6).
- Автоформування фіналу й матчу за 3-тє (Story 4.3): дроти `advanceBracket` (Story 4.1) у персистовані рядки — **на запис і на рендер** (AD-5). **Межа 4.3/4.4 переналаштована** (epics ставив ввід результатів плейофа в 4.4, але всі шляхи були `stage: "GROUP"`-scoped): 4.3 знімає scoping для всіх стадій плейофа; 4.4 → місця 1–4 (`playoffPlacements`) + гейт правки півфіналу після зіграного наступного матчу. **Дві міграції** (`20260907130000` + `..140000` fix — SQL NULL-діра: `NULL IN (...)` = NULL, а CHECK проходить на NULL): `match_slot_stage_check` тепер per-stage (`SEMIFINAL ⇔ SF1/SF2`, `THIRD_PLACE ⇔ THIRD_PLACE`, `FINAL ⇔ FINAL`, з `slot IS NOT NULL`). `src/data/matches.ts` — знято `stage: "GROUP"` з `create/replace/deleteMatchResult` + `updateMatchSchedule` (пара `(tournamentId, matchId)` — скоуп); `getStandings`/`listGroupMatchesForTournament`/`hasAnyGroupResult`/`allGroupMatchesPlayed` лишились GROUP-scoped. `src/data/playoff.ts` — `getPlayoffBracket` (read: `advanceBracket` → `PlayoffBracketView` з `matchId`/іменами/рахунком) і `savePlayoffAdvancement` (write: `FOR UPDATE` + **дві плоскі** транзакційні читання (не nested `findMany` — після raw-lock трапляється pg-warning) → `advanceBracket` → per downstream slot: `PLAYED` skip, `READY` create/update `FINAL`/`THIRD_PLACE` рядок, `AWAITING` обнулити учасників). `src/actions/matches.ts` — `enter/edit/removeMatchResult` приймають будь-яку стадію; після результату півфіналу → `savePlayoffAdvancement` (log-and-swallow). Екран матчу — знято `stage !== "GROUP" → notFound`, ярлик стадії. `src/app/.../schedule/page.tsx` — секція «Плейоф» (`PlayoffSchedule`, NEW) у `PLAYOFF`+. `scripts/verify-advance-bracket.mts` (NEW). `bracket.ts`/`tournamentState.ts` не чіпалися. Публічна сітка/компонент Bracket — 4.6; місця 1–4 — 4.4.
- Завершити турнір (Story 4.5): дріт переходу `PLAYOFF → COMPLETED`. **Без міграції, без нового роуту, без нового домен-модуля.** `src/domain/tournamentState.ts` — нова чиста `checkCanEditResults(state): ResultEditCheck` (форма `checkCanRedraw`): `COMPLETED` → блок «Турнір завершено — результати редагувати не можна.», решта станів → ok. Це резолюція PRD Open Question #3 для v1 (повний блок); Open Question #4 (скасувати завершення) лишається відкритим — поза обсягом. `TRANSITIONS`/`PRECONDITIONS`/`checkTransition` **не чіпалися** — ребро `COMPLETED` і предикат на `ctx.finalAndThirdPlacePlayed` існували зі Story 2.3. `src/data/matches.ts` — `finalAndThirdPlacePlayed(tournamentId, client?)` (форма `allGroupMatchesPlayed` — обидва `FINAL` і `THIRD_PLACE` мають `SetScore`); `getMatchForResult` тепер вибирає `tournament.state`. `src/actions/tournaments.ts` — `transitionTournament` заповнює `context.finalAndThirdPlacePlayed` для `COMPLETED` (перший реальний виклик цієї дії — з `FinishTournamentButton`) + ревалідує `/admin/tournaments`, `…/schedule`, публічний роут турніру. `src/actions/matches.ts` — `checkCanEditResults(tournament.state)` гейт у `enter`/`edit`/`removeMatchResult` **і** `scheduleMatch` (замок розкладу в `COMPLETED` — рішення за EXPERIENCE «усі дії редагування зникають», не буквальний AC epics). `FinishTournamentButton` (`tournament-actions.tsx`) — `ConfirmDialog` (`destructive`) + `checkTransition` disabled/підпис «Доступно коли зіграно фінал і матч за 3-тє місце»; секція `PLAYOFF` на `[id]/page.tsx`. `COMPLETED`: банер «Турнір завершено» на публічній сторінці й в адмінці; `TournamentForm` locked повністю; `MatchScheduleList` (новий проп `locked`) ховає форми розкладу; `MatchResultForm` (новий проп `lockedReason`) / `MatchResultPanel` (`lockedReason` зі Story 4.4) — неактивні. `/classic` **не** прибирає завершені турніри (рішення Story 4.7). `scripts/verify-finish-tournament.mts` (NEW). `pnpm test` 169 (+2). Публічна сітка — 4.6; `/archive` — 4.7. **Рев'ю коду**: `updateTournament` тепер теж відмовляє в `COMPLETED` (реквізити/`scoringPreset` архівного турніру заморожені — інакше змінилася б публічна таблиця); `TournamentForm` при повному замку ховає кнопку сабміту й показує коректну підказку (`lockedHint`); банер винесено в спільний `src/components/completed-banner.tsx` (`role="status"`); `transitionTournament(…COMPLETED)` ревалідує й екрани матчів.

## Domain boundaries (Story 1.3)

- Layer dirs: `src/domain` (pure core), `src/data` (sole Prisma owner + shared client instance), `src/actions` (Server Actions), `src/auth`. Each carries a `README.md`; `src/README.md` has the layer map + dependency direction. `ARCHITECTURE-SPINE.md` (AD-2/AD-3/AD-11) is the authoritative version.
- `eslint.config.mjs` enforces (lint error, alias + relative forms) via `no-restricted-imports` (resolver-independent) + `import/no-restricted-paths` (`eslint-plugin-import`, bundled by `eslint-config-next` — no added dependency):
  - `src/domain/**` — no `next`, no Prisma client, no `react`, no other `src/` layer (incl. `src/lib`).
  - Prisma client (`@/generated/prisma`, `@prisma/client`) — only under `src/data/**`.
  - `src/data/**` — no `actions`/`auth`/`app`/`components`, no `next`/`react`.
  - `src/auth/**` — may import only `src/data` (Better Auth gets the `PrismaClient` instance from there).
- **Open item:** `src/data → src/domain` is *not* blocked (nor is `src/components → src/domain`). AD-3 as written forbids `data → domain`, but `epics.md` Story 3.2 puts `getStandings()` in `src/data` "через `src/domain`", and Story 2.4 already imports the `NewTournamentInput` type into `src/data/tournaments.ts` and the enum consts into `src/components/tournament-form.tsx` (type/const only, no logic). Reconcile in Epic 3 (spine edit, or move read-time computation to the read path per AD-5).

## Hosting

- **Production:** https://cherkasy-volley.vercel.app (Vercel, team `volley3`, project `cherkasy-volley`, region `fra1`). Auto-deploys on push to `main`.
- CLI: `npx vercel link --yes --project cherkasy-volley` links to `volley3/cherkasy-volley` (writes `.vercel/`, `.env.local` — both gitignored).
- Neon Postgres: provisioned via Vercel → Storage (project `twilight-dust-91359102`, prod branch `main`). **Since 2026-09-04, `.env.local`'s `DATABASE_URL`/`DATABASE_URL_UNPOOLED` point at the `dev` branch, not `main`** (see the "Neon `dev` branch" bullet in Stack status) — running `npx vercel env pull .env.local` again silently repoints them back at prod; pull to a different filename (`.env.production.local`) instead if you need prod creds for a one-off check.
- Vercel **Production** env (Story 1.5, set via `vercel env add`): `DATABASE_URL` + `DATABASE_URL_UNPOOLED` as **plain** vars (the Neon integration's Secret `DATABASE_URL` did not reach the serverless runtime); `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `BETTER_AUTH_SECRET`, `DIRECT_URL` sensitive; `BETTER_AUTH_URL=https://cherkasy-volley.vercel.app`. Module-level env guards in `src/data/client.ts` / `src/auth/auth.ts` skip `NEXT_PHASE === "phase-production-build"` (build can't see runtime-only vars).

<!-- /bmad:manual -->
