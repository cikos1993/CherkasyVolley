---
topic: Cherkasy Volleyball Federation Tournament Platform PRD
updated: 2026-09-02T00:30
---

- (event) run started: bmad-prd create intent, forwarded from deprecated bmad-create-prd shim.
- (note by coach) tooling gap: no `uv`/Python on this machine; memlog.py cannot run. Memlog maintained by hand in the script's format.
- (note) primary input: product brief at _bmad-output/planning-artifacts/briefs/brief-untitled-2026-08-31/brief.md (+ addendum.md, .memlog.md). Brief is finalized in Ukrainian, status draft.
- (decision) PRD document language: Ukrainian (matches the brief; user confirmed Ukrainian for docs this project).
- (decision by user) Working mode: Fast path.
- (decision by user) Player fields "спортивний розряд" and "амплуа" are free text entered at creation (no enums). A player record belongs to exactly one team within one tournament (roster is per team-per-tournament, no global player registry).
- (decision by user) A team identity can participate across tournaments (e.g. championship and cup), but appears once per tournament. Roster is specific to each tournament entry.
- (decision by user) Schedule: system generates round-robin pairings per group after the draw; admin only fills in date, time and venue (venue = free text) per match. Admin does not create matches manually.
- (decision by user) Match result: record per-set scores (points per set) AND the sets tally.
- (decision by user) Tournament enters the archive only after admin presses "завершити турнір" (finish tournament).
- (decision by user) UI language: Ukrainian only for v1.
- (assumption by coach) Cup (knockout) format stays out of v1 per the brief; team-reuse-across-types is a data-model note for later. Playoff matches are single matches (not series). Draw can be re-run until fixtures/results exist. To confirm at review.
- (event) Fast-path draft written: prd.md (Ukrainian) — 10 features, FR-1..FR-25, NFR-1..7, constraints (minors' privacy), non-goals, MVP scope, SM-1..4 + 2 counter-metrics, 7 open questions, assumptions index. addendum.md — tech direction, data-model sketch, scoring-engine notes, score validation draft, UJ->FR maps.
- (assumption) key [ПРИПУЩЕННЯ] tags flagged for review: role removal secondary; results locked after finish; draw unseeded; classic-set validation rules; name-based final tiebreak; single-group-only playoff for v1; drafts hidden from viewers; playoff matches are single games.
- (event) draft ready for user review (Fast path iterate).
- (decision by user) Player: only ПІБ required; DOB (and all other fields) optional, shown only if entered. FR-10, Glossary, addendum updated.
- (decision by user) No system-enforced uniqueness of players/teams across tournaments — admin discipline. Veterans tournaments explicitly allow the same player across multiple teams.
- (decision by user) Draft-state tournaments are hidden from viewers entirely (list + direct link). [ПРИПУЩЕННЯ] tags removed, made firm.
- (decision by user) Set target points: custom preset to 25; Veterans' Championship sets to 15. Classic deciding 5th set always to 15. Hard-coded by tournament type for v1; Open Question #5 tracks whether it must become an explicit field.
- (change) Open Questions renumbered: dropped "drafts visibility" and "decider set" (resolved); minors'-privacy dropped as blocker (resolved: DOB optional, consent = Federation's off-platform process). Remaining: multi-group playoff (#1, flagged as risk), group legs/qualification (#2), edit-in-finished (#3), un-finish tournament (#4), set-target field (#5).
- (event) user did not answer the multi-group-playoff question — coach re-asking; it is the one downstream-blocking open item.
- (decision by user) Multi-group playoff: option 2 — v1 supports exactly ONE group per tournament; multi-group formats wait for the regulation. FR-4/FR-11/FR-19, Glossary, UJ-1 (rewritten to 1 group of 6), scope and assumptions index all updated to firm single-group scope.
- (event) Fast-path iteration complete; all raised questions resolved except deferred Open Questions #1-#5 (regulation-dependent / minor). Proceeding to Finalize.
- (event) Finalize: single-group cleanup pass across prd.md (§4.2/4.4/4.8, Glossary, UJ-1, scope, assumptions) + addendum data model. Light editorial structure/prose pass applied inline in place of the full multi-agent reviewer gate — hobby/solo scope; user can run `bmad-review` explicitly if desired.
- (event) Input reconciliation: product brief is the only user-supplied input and is upstream of this PRD (same author, same session) — no divergence; qualitative intent (custom design later, low-ops, "everything in one place") carried into §1/§5/addendum.
- (event) PRD finalized. status: final.
